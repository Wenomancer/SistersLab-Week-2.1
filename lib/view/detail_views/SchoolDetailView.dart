import 'package:flutter/material.dart';

class SchoolDetailView extends StatelessWidget {
  const SchoolDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
    child: Text('School Detail View'),
    );
  }
}
