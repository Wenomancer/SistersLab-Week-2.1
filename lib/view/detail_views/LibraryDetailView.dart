import 'package:flutter/material.dart';

class LibraryDetailView extends StatelessWidget {
  const LibraryDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Library Detail View'),
    );
  }
}
