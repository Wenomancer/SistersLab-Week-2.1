import 'package:flutter/material.dart';

class BusinessDetailView extends StatelessWidget {
  const BusinessDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Business Detail View'),
    );
  }
}
